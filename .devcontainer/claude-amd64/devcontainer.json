{
  "//": "GENERATED FILE -- do not edit. Source: .devcontainer/claude/devcontainer.json. Regenerate: python3 scripts/generate_devcontainer_arch_overlays.py generate. This overlay pins --platform=linux/amd64 for explicit cross-arch selection; see docs/runbooks/devcontainers.md.",
  "name": "claude-md Claude (linux/amd64)",
  "image": "ghcr.io/tvna/claude-md-devcontainer-claude:4c085e48866c3346b64254b545f64ca501bea204",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "initializeCommand": "bash .devcontainer/scripts/check-gh-config-permissions.sh ${HOME}/.config/gh && bash .devcontainer/scripts/ensure-agent-image.sh claude --platform linux/amd64",
  "remoteUser": "claude",
  "updateRemoteUserUID": false,
  "runArgs": [
    "--platform=linux/amd64",
    "--cap-add=NET_ADMIN",
    "--cap-add=BPF",
    "--cap-add=PERFMON"
  ],
  "mounts": [
    "source=claude-md-claude-session,target=/home/claude/.claude,type=volume",
    "source=${localEnv:HOME}/.config/gh,target=/home/claude/.config/gh,type=bind,consistency=cached"
  ],
  "remoteEnv": {
    "AGENT_CONTAINER": "claude"
  },
  "postCreateCommand": "bash .devcontainer/scripts/prepare-agent-workspace.sh claude && nix develop .#claude --command uv sync --locked --group local && bash .devcontainer/scripts/install-agent-cli.sh claude && bash .devcontainer/scripts/configure-agent-runtime.sh claude",
  "postStartCommand": "if [ \"${DEVCONTAINER_APPLY_EGRESS_ALLOWLIST:-1}\" = \"1\" ]; then nix develop .#network --command bash .devcontainer/scripts/apply-egress-allowlist.sh .devcontainer/network/claude.allowlist; fi",
  "customizations": {
    "vscode": {
      "extensions": [
        "jnoortheen.nix-ide",
        "ms-python.python",
        "charliermarsh.ruff"
      ]
    }
  }
}
