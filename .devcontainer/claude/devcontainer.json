{
  "name": "claude-md Claude",
  "image": "ghcr.io/tvna/claude-md-devcontainer-claude:14ca7ebd13b70aa809eded6af2118e6080d19fc1",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "initializeCommand": "bash .devcontainer/scripts/check-gh-config-permissions.sh ${HOME}/.config/gh && bash .devcontainer/scripts/ensure-agent-image.sh claude && bash .devcontainer/scripts/prepare-signing-keys.sh",
  "remoteUser": "claude",
  "updateRemoteUserUID": false,
  "runArgs": [
    "--cap-add=NET_ADMIN",
    "--cap-add=BPF",
    "--cap-add=PERFMON"
  ],
  "mounts": [
    "source=claude-md-claude-session,target=/home/claude/.claude,type=volume",
    "source=${localEnv:HOME}/.config/gh,target=/home/claude/.config/gh,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.ssh/devcontainer-signing-keys,target=/home/claude/.ssh/devcontainer-signing-keys,type=bind,consistency=cached,readonly"
  ],
  "remoteEnv": {
    "AGENT_CONTAINER": "claude"
  },
  "postCreateCommand": "bash .devcontainer/scripts/prepare-agent-workspace.sh claude && nix develop .#claude --command uv sync --locked --group local && bash .devcontainer/scripts/install-agent-cli.sh claude && bash .devcontainer/scripts/configure-agent-runtime.sh claude",
  "postStartCommand": "if [ \"${DEVCONTAINER_APPLY_EGRESS_ALLOWLIST:-1}\" = \"1\" ]; then nix develop .#network --command bash .devcontainer/scripts/apply-egress-allowlist.sh .devcontainer/network/shared.allowlist; fi",
  "customizations": {
    "vscode": {
      "extensions": [
        "jnoortheen.nix-ide",
        "ms-python.python",
        "charliermarsh.ruff"
      ]
    }
  }
}
