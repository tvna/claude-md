{
  "name": "claude-md Codex",
  "image": "ghcr.io/tvna/claude-md-devcontainer-codex:edc6530f8f9cc734479e4970e5583b6785d90d9f",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "initializeCommand": "bash .devcontainer/scripts/check-gh-config-permissions.sh ${HOME}/.config/gh && bash .devcontainer/scripts/ensure-agent-image.sh codex",
  "remoteUser": "codex",
  "updateRemoteUserUID": false,
  "runArgs": [
    "--cap-add=NET_ADMIN",
    "--cap-add=BPF",
    "--cap-add=PERFMON",
    "--security-opt=seccomp=unconfined"
  ],
  "mounts": [
    "source=claude-md-codex-session,target=/home/codex/.codex,type=volume",
    "source=${localEnv:HOME}/.config/gh,target=/home/codex/.config/gh,type=bind,consistency=cached"
  ],
  "remoteEnv": {
    "AGENT_CONTAINER": "codex"
  },
  "postCreateCommand": "bash .devcontainer/scripts/prepare-agent-workspace.sh codex && nix develop .#codex --command uv sync --locked --group local && bash .devcontainer/scripts/install-agent-cli.sh codex && bash .devcontainer/scripts/configure-agent-runtime.sh codex",
  "postStartCommand": "if [ \"${DEVCONTAINER_APPLY_EGRESS_ALLOWLIST:-1}\" = \"1\" ]; then nix develop .#network --command bash .devcontainer/scripts/apply-egress-allowlist.sh .devcontainer/network/codex.allowlist; fi",
  "customizations": {
    "vscode": {
      "extensions": [
        "jnoortheen.nix-ide",
        "ms-python.python",
        "charliermarsh.ruff"
      ]
    }
  }
}
