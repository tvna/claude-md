# Codecov configuration. The authoritative pass/fail gate remains the
# local [tool.coverage.report].fail_under value defined in pyproject.toml
# and enforced by pytest --cov in .github/workflows/post-merge.yml. Every Codecov
# status here is informational so this layer provides dashboard and
# trend history only, never a separate blocking signal. See #462.
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        informational: true
    patch:
      default:
        target: auto
        threshold: 0%
        informational: true
comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: true
ignore:
  - "tests/"
github_checks:
  annotations: true
