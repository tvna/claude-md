.github/workflows/apply-rulesets.yml   @tvna
.github/rulesets/**                    @tvna
.github/CODEOWNERS                     @tvna
docs/runbooks/rulesets.md              @tvna
/.gitapex/                             @tvna
# Only the machine-written TOML snapshot is exempted, scoped to the exact file:
# it is generated by the post-merge automation
# (scripts/scan_module_size_distribution.py) and published via the
# chore/update-generated-docs bot PR, so an owner-less exact-file pattern lets
# bot_pr_automerge.py reach a clean mergeable_state (require_code_owner_review in
# .github/rulesets/main.json). The sibling module-size-distribution.schema.json
# is hand-maintained validation policy consumed by scan_gitapex_schema.py, so it
# stays code-owner-covered by the /.gitapex/ rule above rather than being
# un-owned along with the whole snapshots/ directory. Refs #2364.
/.gitapex/snapshots/module-size-distribution.toml
