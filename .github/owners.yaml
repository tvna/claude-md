# Maps CODEOWNERS handles to ISO-639-1 language codes used by
# scripts/plan_language_context.py (SessionStart hook). Keys retain
# the literal '@' prefix so equality with CODEOWNERS handles is exact.
# Refs #211.
"@tvna": ja
