name: Apply labels

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Plan only (diff + intent, no live changes)'
        type: boolean
        required: true
        default: true
      prune:
        description: 'Also DELETE labels present on GitHub but absent from SoT (destructive on existing issues)'
        type: boolean
        required: true
        default: false

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  GH_TOKEN: ${{ secrets.LABELS_PAT }}
  REPO: ${{ github.repository }}

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: labels-apply
    steps:
      - name: Guard dispatch ref
        if: github.ref != 'refs/heads/main'
        run: |
          echo "::error::apply-labels must be dispatched against main (got ${{ github.ref }})."
          exit 1

      - name: Guard LABELS_PAT
        run: |
          if [ -z "${GH_TOKEN}" ]; then
            echo "::error::LABELS_PAT secret is not set. See docs/runbooks/issue-triage.md §\"Required secret\"."
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Validate label-policy.toml
        # Refs #2499: label-policy.toml is the single label SoT; the live
        # catalog is derived from its status in {keep, rename} entries by
        # labels_apply.load_sot_from_policy.
        run: |
          set -euo pipefail
          python3 scripts/labels_apply.py validate \
            --policy .github/label-policy.toml

      - name: Plan and apply labels
        env:
          INPUT_DRY_RUN: ${{ inputs.dry_run }}
          INPUT_PRUNE: ${{ inputs.prune }}
        run: |
          set -euo pipefail
          COMMAND="apply"
          if [ "$INPUT_DRY_RUN" = "true" ]; then
            COMMAND="plan"
          fi
          python3 scripts/labels_apply.py "$COMMAND" \
            --repo "$REPO" \
            --policy .github/label-policy.toml \
            --prune "$INPUT_PRUNE" \
            --dry-run "$INPUT_DRY_RUN" \
            --summary-file "$GITHUB_STEP_SUMMARY"
