[project]
name = "claude-md"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
exclude-newer = "14 days"
required-version = ">=0.11.8"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
